const propertiesService = require('../lib/services/properties-service');
const assert = require('assert');

describe('properties-service', function () {
	describe('getProperties', function () {
		it('should return environtment variables', async () => {
			try {
				const serviceName = '';
				const region = '';
			} catch (error) {
				assert.fail();
			}
		});
	});
});
