language: node_js
node_js:
  - 12
cache:
  directories:
    - "node_modules"
install:
  - npm install
script:
  - npm run build
deploy:
  provider: releases
  api_key: $GITHUB_KEY
  file:
    - ecs-env-linux
    - ecs-env-macos
    - ecs-env-win
